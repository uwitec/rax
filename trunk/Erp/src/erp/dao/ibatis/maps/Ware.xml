<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Ware">

	<typeAlias alias="Ware" type="erp.model.Ware" />

	<insert id="create" parameterClass="Ware">
		<![CDATA[
		INSERT INTO erp.ware (category_id,name,cost,price,barcode,number,status,lowest_cost) 
		VALUES (#categoryId#,#name#,#cost#,#price#,#barcode#,#number#,#status#,#lowestCost#)
		]]>
		<selectKey resultClass="int" keyProperty="id">
			<![CDATA[
			SELECT currval('erp.ware_id_seq')
			]]>
		</selectKey>
	</insert>

	<select id="read" parameterClass="int" resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,lowest_cost AS lowestCost
		FROM erp.ware
		WHERE id=#id#
		]]>
	</select>

	<update id="update" parameterClass="Ware">
		<![CDATA[
		UPDATE erp.ware SET category_id=#categoryId#,name=#name#,cost=#cost#,price=#price#, 
		barcode=#barcode#,number=#number#,status=#status#,lowest_cost=#lowestCost#
		WHERE id=#id#
		]]>
	</update>

	<delete id="delete" parameterClass="int">
		<![CDATA[
		DELETE FROM erp.ware WHERE id=#id#
		]]>
	</delete>

	<select id="count" parameterClass="map" resultClass="int">
		<![CDATA[
		SELECT COUNT(*) AS NUM FROM erp.ware
		]]>
		<dynamic>
			<isGreaterThan prepend="WHERE" property="status"
				compareValue="-1">
				status=#status#
			</isGreaterThan>
		</dynamic>
	</select>

	<select id="list" parameterClass="map" resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS
		categoryId,name,cost,price,barcode,number,status,lowest_cost AS lowestCost
		FROM erp.ware
		]]>
		<dynamic>
			<isGreaterThan prepend="WHERE" property="status"
				compareValue="-1">
				status=#status#
			</isGreaterThan>
		</dynamic>
		<![CDATA[
		ORDER BY name LIMIT #number# OFFSET #index#
		]]>
	</select>

	<select id="listByCategoryId" parameterClass="map"
		resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS
		categoryId,name,cost,price,barcode,number,status,lowest_cost AS	lowestCost
		FROM erp.ware WHERE category_id=#id#
		]]>
		<dynamic>
			<isGreaterThan prepend="AND" property="status"
				compareValue="-1">
				status=#status#
			</isGreaterThan>
		</dynamic>
		<![CDATA[
		ORDER BY name
		]]>
	</select>

	<select id="findByBarcode" parameterClass="string"
		resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,lowest_cost AS lowestCost
		FROM erp.ware
		WHERE barcode=#barcode#
		]]>
	</select>

	<select id="findByKeywords" parameterClass="list"
		resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,lowest_cost AS lowestCost
		FROM erp.ware
		]]>
		<dynamic prepend="WHERE">
			<iterate open="(" close=")" conjunction="AND">
				strpos(lower(name), #keywordList[]#) > 0
			</iterate>
		</dynamic>
		<![CDATA[
		ORDER BY name
		]]>
	</select>

	<select id="findAdvanced" parameterClass="map" resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,lowest_cost AS lowestCost
		FROM erp.ware
		WHERE
		]]>
		<dynamic>
			<iterate open="(" close=")" conjunction="AND"
				property="keywords">
				strpos(lower(name), #keywords[]#) > 0
			</iterate>
		</dynamic>
		<dynamic prepend="AND">number BETWEEN #min# AND #max#</dynamic>
		<![CDATA[
		ORDER BY name
		]]>
	</select>

	<select id="findByNum" parameterClass="map" resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,lowest_cost AS lowestCost
		FROM erp.ware
		WHERE number BETWEEN #min# AND #max#
		]]>
	</select>

	<select id="fullTextSearch" parameterClass="string"
		resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,lowest_cost AS lowestCost, ts_rank (idx_name, q) AS r
		FROM erp.ware, to_tsquery(#content#) q
		WHERE idx_name @@ q
		ORDER BY r DESC
		LIMIT 12
		]]>
	</select>

	<update id="updateFullTextIndex" parameterClass="map">
		<![CDATA[
		UPDATE erp.ware SET idx_name=to_tsvector(#tokenize#)
		WHERE id=#id#
		]]>
	</update>

</sqlMap>
