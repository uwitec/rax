<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Ware">

	<typeAlias alias="Ware" type="erp.model.Ware" />

	<insert id="create" parameterClass="Ware">
		<![CDATA[
		INSERT INTO erp.ware (category_id,name,cost,price,barcode,number,status,last_cost) 
		VALUES (#categoryId#,#name#,#cost#,#price#,#barcode#,#number#,#status#,#lastCost#)
		]]>
		<selectKey resultClass="int" keyProperty="id">
			<![CDATA[
			SELECT currval('erp.ware_id_seq')
			]]>
		</selectKey>
	</insert>

	<select id="read" parameterClass="int" resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,last_cost AS lastCost
		FROM erp.ware
		WHERE id=#id#
		]]>
	</select>

	<update id="update" parameterClass="Ware">
		<![CDATA[
		UPDATE erp.ware SET category_id=#categoryId#,name=#name#,cost=#cost#,price=#price#, 
		barcode=#barcode#,number=#number#,status=#status#,last_cost=#lastCost#
		WHERE id=#id#
		]]>
	</update>

	<delete id="delete" parameterClass="int">
		<![CDATA[
		DELETE FROM erp.ware WHERE id=#id#
		]]>
	</delete>

	<select id="count" parameterClass="map" resultClass="int">
		SELECT COUNT(*) AS NUM FROM erp.ware
		<dynamic>
			<isGreaterThan prepend="WHERE" property="status"
				compareValue="-1">
				status=#status#
			</isGreaterThan>
		</dynamic>
	</select>

	<select id="list" parameterClass="map" resultClass="Ware">
		SELECT id,category_id AS
		categoryId,name,cost,price,barcode,number,status,last_cost AS
		lastCost FROM erp.ware
		<dynamic>
			<isGreaterThan prepend="WHERE" property="status"
				compareValue="-1">
				status=#status#
			</isGreaterThan>
		</dynamic>
		ORDER BY name LIMIT #number# OFFSET #index#
	</select>

	<select id="listByCategoryId" parameterClass="map"
		resultClass="Ware">
		SELECT id,category_id AS
		categoryId,name,cost,price,barcode,number,status,last_cost AS
		lastCost FROM erp.ware WHERE category_id=#id#
		<dynamic>
			<isGreaterThan prepend="AND" property="status"
				compareValue="-1">
				status=#status#
			</isGreaterThan>
		</dynamic>
		ORDER BY name
	</select>

	<select id="findByBarcode" parameterClass="string"
		resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,last_cost AS lastCost
		FROM erp.ware
		WHERE barcode=#barcode#
		]]>
	</select>

	<select id="findByKeywords" parameterClass="list"
		resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,last_cost AS lastCost
		FROM erp.ware
		]]>
		<dynamic prepend="WHERE">
			<iterate open="(" close=")" conjunction="AND">
				strpos(name, #keywordList[]#) > 0
			</iterate>
		</dynamic>
		<![CDATA[
		ORDER BY name
		]]>
	</select>

	<select id="findAdvanced" parameterClass="map" resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,last_cost AS lastCost
		FROM erp.ware
		WHERE
		]]>
		<dynamic>
			<iterate open="(" close=")" conjunction="AND" property="keywords">
				strpos(name, #keywords[]#) > 0
			</iterate>
		</dynamic>
		<dynamic prepend="AND">
			number BETWEEN #min# AND #max#
		</dynamic>
		<![CDATA[
		ORDER BY name
		]]>
	</select>

	<select id="findByNum" parameterClass="map" resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,last_cost AS lastCost
		FROM erp.ware
		WHERE number BETWEEN #min# AND #max#
		]]>
	</select>

	<select id="fullTextSearch" parameterClass="string"
		resultClass="Ware">
		<![CDATA[
		SELECT id,category_id AS categoryId,name,cost,price,barcode,number,status,last_cost AS lastCost
		FROM erp.ware, to_tsquery(erp.querycharseg(#content#, 1)) q
		WHERE idx_name @@ q
		ORDER BY ts_rank (idx_name, q) DESC
		LIMIT 12
		]]>
	</select>

</sqlMap>