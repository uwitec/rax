<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="LinkCategory">

	<typeAlias alias="LinkCategory" type="rax.model.LinkCategory" />

	<insert id="createLinkCategory" parameterClass="LinkCategory">
		<![CDATA[
		INSERT INTO rax_link (category_id, title, address, summary, pub) 
		VALUES (#category_id#, #title#, #address#, #summary#, #pub#)
		]]>
	</insert>

	<select id="readLinkCategory" parameterClass="Long" resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		WHERE id=#id#
		]]>
	</select>

	<update id="updateLinkCategory" parameterClass="LinkCategory">
		<![CDATA[
		UPDATE rax_link SET category_id=#categoryId#, title=#title#,
		address=#address#, summary=#summary#, pub=#pub#
		WHERE id=#id#
		]]>
	</update>

	<delete id="deleteLinkCategory" parameterClass="Long">
		<![CDATA[
		DELETE rax_link WHERE id=#id#
		]]>
	</delete>

	<delete id="deleteLinkCategoryByCategoryId" parameterClass="Long">
		<![CDATA[
		DELETE rax_link WHERE category_id=#id#
		]]>
	</delete>

	<select id="countAllLinkCategory" resultClass="Long">
		<![CDATA[
		SELECT COUNT(*) AS NUM FROM rax_link
		]]>
	</select>

	<select id="countPubLinkCategory" resultClass="Long">
		<![CDATA[
		SELECT COUNT(*) AS NUM FROM rax_link WHERE pub=TRUE
		]]>
	</select>

	<select id="countAllLinkCategoryByCategoryId" parameterClass="Long"
		resultClass="Long">
		<![CDATA[
		SELECT COUNT(*) AS NUM FROM rax_link 
		WHERE category_id=#id#
		]]>
	</select>

	<select id="countPubLinkCategoryByCategoryId" parameterClass="Long"
		resultClass="Long">
		<![CDATA[
		SELECT COUNT(*) AS NUM FROM rax_link 
		WHERE pub=TRUE AND category_id=#id#
		]]>
	</select>

	<parameterMap id="listParam" class="java.util.Map">
		<parameter property="index" jdbcType="integer" javaType="Long"
			nullValue="0" />
		<parameter property="number" jdbcType="integer"
			javaType="Integer" nullValue="20" />
	</parameterMap>

	<select id="listLinkCategory" parameterMap="listParam" resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		LIMIT #number# OFFSET #index#
		]]>
	</select>

	<select id="listPubLinkCategory" parameterMap="listParam"
		resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		WHERE pub=TRUE
		LIMIT #number# OFFSET #index#
		]]>
	</select>

	<select id="listLinkCategoryByCategoryId" parameterMap="listParam"
		resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		WHERE category_id=#id#
		LIMIT #number# OFFSET #index#
		]]>
	</select>

	<select id="listPubLinkCategoryByCategoryId" parameterMap="listParam"
		resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		WHERE pub=TRUE AND category_id=#id#
		LIMIT #number# OFFSET #index#
		]]>
	</select>

	<select id="listAllLinkCategory" resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		]]>
	</select>

	<select id="listAllPubLinkCategory" resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		WHERE pub=TRUE
		]]>
	</select>

	<select id="listAllLinkCategoryByCategoryId" parameterClass="Long"
		resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		WHERE category_id=#id#
		]]>
	</select>

	<select id="listAllPubLinkCategoryByCategoryId" parameterClass="Long"
		resultClass="LinkCategory">
		<![CDATA[
		SELECT id,category_id,title,address,summary,pub 
		FROM rax_link
		WHERE pub=TRUE AND category_id=#id#
		]]>
	</select>

	<select id="lastInsert" resultClass="Long">
		<![CDATA[
		SELECT last_insert_id() AS NUM
		]]>
	</select>
	
</sqlMap>